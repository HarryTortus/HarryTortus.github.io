<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HarryTortus | Creative Web Projects</title>
    <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@400;700&family=Open+Sans:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-bg: #ede3df; /* Light beige/off-white */
            --accent-color: #4c6121; /* Dark green/olive */
            --light-text: #ede3df; /* For text on dark backgrounds */
            --dark-text: #4c6121; /* For text on light backgrounds */
        }

        body {
            margin: 0;
            font-family: 'Open Sans', sans-serif;
            background-color: var(--primary-bg);
            color: var(--dark-text);
            line-height: 1.6;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        header {
            background-color: var(--accent-color);
            padding: 1.5rem 0;
            text-align: center;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        .logo {
            font-family: 'Oswald', sans-serif;
            font-size: 2.5em;
            font-weight: 700;
            color: var(--light-text);
            text-decoration: none;
            letter-spacing: 2px;
            text-shadow: 1px 1px 3px rgba(0,0,0,0.1);
        }

        main {
            flex-grow: 1;
            width: 90%;
            max-width: 1000px;
            margin: 0 auto;
            padding: 2rem 0;
        }

        .hero-section {
            text-align: center;
            padding: 3rem 0;
            margin-bottom: 3rem;
            border-bottom: 1px solid rgba(76, 97, 33, 0.2); /* Soft separator */
        }

        .hero-section h1 {
            font-family: 'Oswald', sans-serif;
            font-size: 3.5em;
            font-weight: 700;
            margin-bottom: 0.5rem;
            color: var(--accent-color);
            letter-spacing: 1.5px;
        }

        .hero-section p {
            font-size: 1.2em;
            color: rgba(76, 97, 33, 0.8); /* Slightly lighter text */
            max-width: 700px;
            margin: 0 auto;
        }

        .projects-section {
            padding: 2rem 0;
        }

        .projects-section h2 {
            font-family: 'Oswald', sans-serif;
            font-size: 2.5em;
            font-weight: 700;
            text-align: center;
            margin-bottom: 2.5rem;
            color: var(--accent-color);
            letter-spacing: 1px;
        }

        .projects-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2.5rem;
            justify-content: center;
            align-items: stretch; /* Ensures cards are same height */
        }

        .project-card {
            background-color: var(--accent-color); /* Dark green background for cards */
            color: var(--light-text); /* Light text on dark card */
            padding: 2rem;
            border-radius: 12px;
            text-decoration: none;
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.15);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            display: flex;
            flex-direction: column;
            justify-content: space-between; /* Pushes content apart if needed */
        }

        .project-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 12px 25px rgba(0, 0, 0, 0.25);
        }

        .project-card h3 {
            font-family: 'Oswald', sans-serif;
            font-size: 1.8em;
            margin-top: 0;
            margin-bottom: 0.8rem;
            color: var(--light-text);
            letter-spacing: 0.8px;
        }

        .project-card p {
            font-size: 1em;
            color: rgba(237, 227, 223, 0.85); /* Slightly muted light text */
        }

        footer {
            background-color: var(--accent-color);
            color: var(--light-text);
            text-align: center;
            padding: 1.5rem 0;
            margin-top: 3rem;
            font-size: 0.9em;
            box-shadow: 0 -2px 5px rgba(0, 0, 0, 0.05);
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .hero-section h1 {
                font-size: 2.8em;
            }
            .hero-section p {
                font-size: 1.1em;
            }
            .projects-section h2 {
                font-size: 2em;
            }
            .project-card {
                padding: 1.5rem;
            }
            main {
                width: 95%;
            }
        }

        @media (max-width: 480px) {
            .hero-section h1 {
                font-size: 2.2em;
            }
            .hero-section p {
                font-size: 1em;
            }
            .logo {
                font-size: 2em;
            }
        }
    </style>
</head>
<body>
    <header>
        <div class="logo">HarryTortus</div>
    </header>

    <main>
        <section class="hero-section">
            <h1>HarryTortus's Digital Canvas</h1>
            <p>Welcome to my space where code meets creativity. Explore interactive art, generative designs, and web experiments.</p>
        </section>

        <section class="projects-section">
            <h2>My Projects</h2>
            <div class="projects-grid">
                <a href="https://harrytortus.github.io/orbital-shapes/" class="project-card">
                    <h3>Orbital Shapes</h3>
                    <p>Dive into mesmerizing planetary motion simulations and explore the dance of celestial bodies.</p>
                </a>
                <a href="https://harrytortus.github.io/line-flow/" class="project-card">
                    <h3>Line Flow</h3>
                    <p>Uncover intricate generative art with dynamic flow fields, bringing visual poetry to life.</p>
                </a>
                </div>
        </section>
    </main>

    <footer>
        <p>&copy; 2025 HarryTortus. All rights reserved.</p>
    </footer>
</body>
</html>
